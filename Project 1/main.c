#include <stdio.h>
#include <stdlib.h>
#include <omp.h>
#include <string.h>


int main(int argc, char **argv){
    char word[256];
    
    double d = atof(argv[2]);
    double epsilon = atof(argv[3]);
    int n = atoi(argv[4]);

}